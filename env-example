CLIENT_ID=xx
TENANT_ID=xx
CLIENT_SECRET=xx
WEBHOOK_URL=https://08a0-81-185-175-161.ngrok-free.app/webhook
BASE_URL=https://08a0-81-185-175-161.ngrok-free.app
CRON_JOB_SCHEDULE=*/10 * * * * *
PROCESS_CRON_JOB_SCHEDULE=*/10 * * * * *
FETCH_CRON_JOB_SCHEDULE=*/10 * * * * *

MESSAGE_PATTERN=agent:deploy

CHAT_ID=48:notes

# Default GitLab connection (legacy support)
# These will be used to create a default connector if none exists
GITLAB_BASE_URL=https://gitlab.com
GITLAB_ACCESS_TOKEN=xx
GITLAB_REPO_NAME=your-repo

# For multi-repository support, configure git connectors via the UI
# Each connector can have its own URL and access token

OPENROUTER_API_KEY=xx

APP_NAME=teams-node-catcher-local

BRANCH_NAMING_CONVENTIONS=GEO-XXX for GeoredV3 features,ADM-XXX for AdminV3 features, GDM-XXX for Citimission features

PRODUCTION_BRANCH=origin/prod
PREPROD_BRANCH=origin/preprod